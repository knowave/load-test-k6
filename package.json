{
    "name": "load-test-k6",
    "version": "1.0.0",
    "main": "dist/main.js",
    "license": "MIT",
    "scripts": {
        "dev": "ts-node-dev --respawn --transpile-only src/main.ts",
        "debug": "ts-node-dev --respawn --transpile-only --inspect -- src/main.ts",
        "build": "tsc",
        "prod": "yarn build && node dist/main.js"
    },
    "dependencies": {
        "express": "^5.1.0"
    },
    "devDependencies": {
        "@types/express": "^5.0.5",
        "@types/node": "^24.10.1",
        "ts-node-dev": "^2.0.0",
        "typescript": "^5.9.3"
    }
}
